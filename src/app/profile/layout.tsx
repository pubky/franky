import * as Templates from '@/templates';

export default function ProfileLayout({ children }: { children: React.ReactNode }) {
  return <Templates.ProfileLayout>{children}</Templates.ProfileLayout>;
}
