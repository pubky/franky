/** Delay before triggering onBlur callback (allows clicking on suggestions/emoji) */
export const BLUR_DELAY_MS = 200;

/** Base CSS classes for the tag input field */
export const INPUT_BASE_CLASSES =
  'flex-1 bg-transparent p-0 text-sm leading-5 font-bold caret-white border-none shadow-none ring-0 outline-none hover:outline-none focus:ring-0 focus:ring-offset-0 focus:outline-none placeholder:font-bold';
