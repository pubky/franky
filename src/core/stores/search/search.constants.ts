/**
 * Search Store Constants
 *
 * Constants for search-related functionality including recent searches
 * and active search tag limits.
 */

/** Maximum number of recent searches to store per type (users or tags) */
export const MAX_RECENT_SEARCHES = 5;

/** Maximum number of active search tags allowed */
export const MAX_ACTIVE_SEARCH_TAGS = 3;
