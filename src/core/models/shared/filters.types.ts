/**
 * Timeframe filter options
 */
export enum Timeframe {
  TODAY = 'today',
  MONTH = 'month',
  ALL = 'all',
}

/**
 * Reach filter options
 */
export enum Reach {
  ALL = 'all',
  FOLLOWERS = 'followers',
  FOLLOWING = 'following',
  FRIENDS = 'friends',
  WOT = 'wot',
}
